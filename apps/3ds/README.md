# Nintendo 3DS target

The 3DS client is intentionally deferred until the shared RomM and save-sync
core has been validated by the DSi implementation. It will use its own libctru
build container and platform adapters.

